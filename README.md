
  # Profile Theme Creation

  This is a code bundle for Profile Theme Creation. The original project is available at https://www.figma.com/design/inFAbC38H8xOC8eqGPmt2f/Profile-Theme-Creation.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  